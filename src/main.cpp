#include "curl/curl.h"
#include ""
#include <assert.h>

int main(int argc, char** argv) {
    // curl
    CURL* curl = curl_easy_init();
    assert(curl);

    // glog

    // jsoncpp

    // rdkafka

    return 0;
}
